<odoo>
    <record id="ir_cron_call_endpoint" model="ir.cron">
        <field name="name">Call METRC endpoint</field>
        <field name="model_id" ref="model_stock_lot"/>
        <field name="state">code</field>
        <field name="code">model.cron_call_endpoint()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="nextcall" eval="(DateTime.now().replace(hour=6, minute=0, second=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
    </record>
</odoo>
